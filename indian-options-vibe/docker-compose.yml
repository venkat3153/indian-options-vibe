services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - APP_MODE=paper
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_BASE=http://localhost:8000
